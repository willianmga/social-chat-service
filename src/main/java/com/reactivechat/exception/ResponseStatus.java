package com.reactivechat.exception;

public enum ResponseStatus {
    SUCCESS,
    INVALID_CREDENTIALS,
    INVALID_NAME,
    INVALID_USERNAME,
    INVALID_PASSWORD,
    USERNAME_IN_USE,
    SERVER_ERROR
}
